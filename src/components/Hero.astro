---
// Sección Hero principal
---

<section id="inicio" class="hero">
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <div class="container">
      <h1 class="hero-title">Grupo Scout Hipocampo 503</h1>
      <p class="hero-subtitle">Construir un Mundo Mejor</p>
      <p class="hero-description">
        Educamos en valores a través del escultismo en San Pedro del Pinatar, Murcia.
        Aventura, naturaleza y amistad para todas las edades.
      </p>
      <div class="hero-buttons">
        <a href="#quienes-somos" class="btn">Conócenos</a>
        <a href="#contacto" class="btn btn-outline">Únete al grupo</a>
      </div>
    </div>
  </div>
  <div class="hero-scroll">
    <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <path d="M12 5v14M19 12l-7 7-7-7"/>
    </svg>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    overflow: hidden;
    margin-top: 70px; /* Altura del header fijo */
  }

  /* Patrón de fondo con flor de lis */
  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.05) 0%, transparent 50%),
      radial-gradient(circle at 80% 70%, rgba(255, 255, 255, 0.05) 0%, transparent 50%);
    pointer-events: none;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
      repeating-linear-gradient(
        45deg,
        transparent,
        transparent 100px,
        rgba(255, 255, 255, 0.02) 100px,
        rgba(255, 255, 255, 0.02) 200px
      );
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: var(--spacing-xxl) 0;
  }

  .hero-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    color: var(--color-white);
    margin-bottom: var(--spacing-sm);
    font-weight: 700;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
    animation: fadeInUp 0.8s ease-out;
  }

  .hero-subtitle {
    font-size: clamp(1.2rem, 3vw, 1.8rem);
    color: var(--color-white);
    margin-bottom: var(--spacing-md);
    font-weight: 400;
    opacity: 0.95;
    animation: fadeInUp 1s ease-out;
  }

  .hero-description {
    font-size: clamp(1rem, 2vw, 1.2rem);
    color: var(--color-white);
    max-width: 700px;
    margin: 0 auto var(--spacing-lg);
    line-height: 1.8;
    opacity: 0.9;
    animation: fadeInUp 1.2s ease-out;
  }

  .hero-buttons {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeInUp 1.4s ease-out;
  }

  .hero-buttons .btn {
    padding: var(--spacing-md) var(--spacing-xl);
    font-size: var(--font-size-large);
    background-color: var(--color-white);
    color: var(--color-primary);
    box-shadow: var(--shadow-lg);
  }

  .hero-buttons .btn:hover {
    background-color: var(--color-light-gray);
    transform: translateY(-3px);
  }

  .hero-buttons .btn-outline {
    background-color: transparent;
    border: 2px solid var(--color-white);
    color: var(--color-white);
  }

  .hero-buttons .btn-outline:hover {
    background-color: var(--color-white);
    color: var(--color-primary);
  }

  .hero-scroll {
    position: absolute;
    bottom: var(--spacing-lg);
    left: 50%;
    transform: translateX(-50%);
    color: var(--color-white);
    opacity: 0.7;
    animation: bounce 2s infinite;
    cursor: pointer;
  }

  /* Animaciones */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-10px);
    }
    60% {
      transform: translateX(-50%) translateY(-5px);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero {
      min-height: calc(100vh - 70px);
      margin-top: 70px;
    }

    .hero-content {
      padding: var(--spacing-xl) 0;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: center;
    }

    .hero-buttons .btn {
      width: 100%;
      max-width: 300px;
    }
  }

  @media (max-width: 480px) {
    .hero-buttons .btn {
      padding: var(--spacing-sm) var(--spacing-lg);
      font-size: var(--font-size-base);
    }
  }
</style>
